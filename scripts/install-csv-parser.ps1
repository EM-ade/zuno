# Install CSV parser for NFT metadata parsing
Write-Host "Installing CSV parser..." -ForegroundColor Green
npm install csv-parse

Write-Host "CSV parser installed successfully!" -ForegroundColor Green
